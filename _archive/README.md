# Archived Scripts

These files were moved here on 2026-08-22 during repository cleanup.

They are historical one-off scripts, debug utilities, or duplicate variants that are no longer part of the active pipeline.

**Canonical replacements:**
- Dashboard generation: `build_nonlinkedin_dashboard.py` (root)
- Scraping: `scrape_daily.py` + `jobspy_scraper.py` + `seek_scraper.py` (root)
- Pipeline: `job_pipeline/` directory

To restore any file: `mv _archive/<file> .`
