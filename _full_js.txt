<script>
if (document.cookie.includes('site_access=')) { /* Site gate has already authenticated this browser. */ }
const cards=[...document.querySelectorAll('.card')],q=document.querySelector('#q'),fit=document.querySelector('#fit'),work=document.querySelector('#work'),source=document.querySelector('#source'),verification=document.querySelector('#verification'),stageFilter=document.querySelector('#stage-filter'),sort=document.querySelector('#sort'),resultCount=document.querySelector('#result-count'),stageSummary=document.querySelector('#stage-summary'),roleData=JSON.parse(document.querySelector('#role-data').textContent);let authToken=sessionStorage.getItem('sam-job-dashboard-auth-token')||'',jobIds=new Map(),statuses=new Map();const preferenceKey='sam-job-dashboard-preferences-v2';
const authEmail=document.querySelector('#auth-email'),authPassword=document.querySelector('#auth-password'),authStatus=document.querySelector('#auth-status'),signIn=document.querySelector('#auth-signin'),signOut=document.querySelector('#auth-signout');
const setAuthStatus=(text,ok=false)=>{authStatus.textContent=text;authStatus.style.color=ok?'var(--green)':'var(--muted)';};
const api=(path,options={})=>fetch(path,{...options,headers:{...(options.headers||{}),...(authToken?{Authorization:`Bearer ${authToken}`}:{})}});
const stageOrder=['New','Review','Ready','Applied','Interview','Offer'];
const stageClass=status=>`stage-${String(status||'New').toLowerCase()}`;
const setCardStage=(rid,status)=>{const card=document.querySelector(`#role-${rid}`);if(!card)return;card.classList.remove(...stageOrder.map(stageClass),'stage-rejected');card.classList.add(stageClass(status));const value=card.querySelector('.stage-value');if(value)value.textContent=status;const select=card.querySelector('.status-select');if(select)select.value=status;updateSummary();filter();};
const setRoleStatus=(jobId,status)=>{document.querySelectorAll(`.status-select`).forEach(select=>{const role=roleData.find(r=>r.id===select.dataset.roleId);if(role&&jobIds.get(role.url)===jobId)setCardStage(select.dataset.roleId,status);});};
async function loadSharedState(){if(!authToken)return;const jobsResponse=await api('/api/jobs');if(!jobsResponse.ok)throw new Error('Could not load jobs');const jobs=await jobsResponse.json();jobIds=new Map(jobs.map(job=>[job.application_route,job.id]));const statusResponse=await api('/api/status');if(!statusResponse.ok)throw new Error('Could not load shared statuses');statuses=new Map((await statusResponse.json()).map(item=>[item.job_id,item]));document.querySelectorAll('.status-select').forEach(select=>{const role=roleData.find(r=>r.id===select.dataset.roleId),item=role?statuses.get(jobIds.get(role.url)):null;setCardStage(select.dataset.roleId,item?.status||'New');});}
function savePreferences(){localStorage.setItem(preferenceKey,JSON.stringify({q:q.value,fit:fit.value,work:work.value,source:source.value,verification:verification.value,stage:stageFilter.value,sort:sort.value}))}
function restorePreferences(){try{const p=JSON.parse(localStorage.getItem(preferenceKey)||'{}');q.value=p.q||'';fit.value=p.fit||'';work.value=p.work||'';source.value=p.source||'';verification.value=p.verification||'';stageFilter.value=p.stage||'';sort.value=p.sort||'score'}catch{localStorage.removeItem(preferenceKey)}}
function updateSummary(){const counts=Object.fromEntries(stageOrder.map(stage=>[stage,0]));cards.forEach(c=>{const stage=c.querySelector('.stage-value')?.textContent||'New';if(counts[stage]!==undefined)counts[stage]++});stageSummary.textContent=stageOrder.filter(stage=>counts[stage]).map(stage=>`${stage} ${counts[stage]}`).join(' · ')}
function filter(){const s=q.value.toLowerCase(),f=+fit.value,w=work.value,src=source.value,ver=verification.value,stage=stageFilter.value;const visibleCards=cards.filter(c=>{const matches=(!s||c.innerText.toLowerCase().includes(s))&&(!f||+c.dataset.score>=f)&&(!w||c.dataset.work===w)&&(!src||c.dataset.sourceGroup===src)&&(!ver||c.dataset.verification===ver)&&(!stage||c.querySelector('.stage-value')?.textContent===stage);c.classList.toggle('hidden',!matches);return matches});visibleCards.sort((a,b)=>{if(sort.value==='company')return a.querySelector('.company').textContent.localeCompare(b.querySelector('.company').textContent);if(sort.value==='stage')return stageOrder.indexOf(a.querySelector('.stage-value')?.textContent)-stageOrder.indexOf(b.querySelector('.stage-value')?.textContent);if(sort.value==='posted')return (b.querySelector('.meta')?.textContent||'').localeCompare(a.querySelector('.meta')?.textContent||'');return Number(b.dataset.score)-Number(a.dataset.score)});visibleCards.forEach(c=>c.parentElement.appendChild(c));resultCount.textContent=`${visibleCards.length} of ${cards.length} roles shown`;updateSummary();savePreferences()}
restorePreferences();q.oninput=fit.onchange=work.onchange=source.onchange=verification.onchange=stageFilter.onchange=sort.onchange=filter;document.querySelector('#clear').onclick=()=>{q.value=fit.value=work.value=source.value=verification.value=stageFilter.value='';sort.value='score';localStorage.removeItem(preferenceKey);filter()};filter();
async function saveStage(select,next){const role=roleData.find(r=>r.id===select.dataset.roleId),jobId=role?jobIds.get(role.url):null,previous=(statuses.get(jobId)||{}).status||'New';if(!authToken||!jobId){setCardStage(select.dataset.roleId,previous);alert('Sign in before changing a shared status. No local status was saved.');return}if(next==='Applied'&&!confirm('Mark this role as Applied? Confirm only after you have submitted the application manually.')){setCardStage(select.dataset.roleId,previous);return}select.disabled=true;try{const response=await api('/api/status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({job_id:jobId,status:next})});if(!response.ok)throw new Error((await response.json()).error||`HTTP ${response.status}`);const saved=(await response.json()).status;statuses.set(jobId,{status:saved});setCardStage(select.dataset.roleId,saved);}catch(error){setCardStage(select.dataset.roleId,previous);alert(`Status update failed: ${error.message}`);}finally{select.disabled=false;}}
document.querySelectorAll('.status-select').forEach(select=>select.onchange=()=>saveStage(select,select.value));document.querySelectorAll('.copy-link-btn').forEach(button=>button.onclick=async()=>{const url=`${location.origin}${location.pathname}#role-${button.dataset.roleId}`;try{await navigator.clipboard.writeText(url);const old=button.textContent;button.textContent='Copied';setTimeout(()=>button.textContent=old,1400)}catch{prompt('Copy this role link:',url)}});
document.querySelectorAll('.next-stage-btn').forEach(button=>button.onclick=()=>{const select=document.querySelector(`.status-select[data-role-id="${button.dataset.roleId}"]`),current=select.value;if(current==='Offer'){alert('This role is already at Offer.');return}if(current==='Rejected'){alert('Move a rejected role back to Review with the selector before advancing it.');return}const nextIndex=stageOrder.indexOf(current)+1,next=stageOrder[Math.min(nextIndex,stageOrder.length-1)];saveStage(select,next);});
signIn.onclick=async()=>{signIn.disabled=true;setAuthStatus('Signing in…');try{const configResponse=await fetch('/api/config');if(!configResponse.ok)throw new Error('Public Supabase configuration unavailable');const config=await configResponse.json();const response=await fetch(`${config.supabaseUrl}/auth/v1/token?grant_type=password`,{method:'POST',headers:{'Content-Type':'application/json',apikey:config.supabasePublishableKey},body:JSON.stringify({email:authEmail.value.trim(),password:authPassword.value})});const session=await response.json();if(!response.ok||!session.access_token)throw new Error(session.msg||session.error_description||'Sign-in failed');authToken=session.access_token;sessionStorage.setItem('sam-job-dashboard-auth-token',authToken);authPassword.value='';signIn.hidden=true;signOut.hidden=false;setAuthStatus(`Signed in as ${session.user?.email||authEmail.value}`,true);await loadSharedState();}catch(error){authToken='';sessionStorage.removeItem('sam-job-dashboard-auth-token');signIn.hidden=false;signOut.hidden=true;setAuthStatus(error.message);}finally{signIn.disabled=false;}};
signOut.onclick=()=>{authToken='';sessionStorage.removeItem('sam-job-dashboard-auth-token');statuses.clear();signIn.hidden=false;signOut.hidden=true;setAuthStatus('Signed out.');document.querySelectorAll('.status-select').forEach(select=>setCardStage(select.dataset.roleId,'New'));};
if(authToken){signIn.hidden=true;signOut.hidden=false;setAuthStatus('Restoring session…');loadSharedState().then(()=>setAuthStatus('Signed in.',true)).catch(()=>{authToken='';sessionStorage.removeItem('sam-job-dashboard-auth-token');signIn.hidden=false;signOut.hidden=true;setAuthStatus('Session expired. Sign in again.');});}
const automationStatus=document.querySelector('#automation-status');document.querySelectorAll('.generate-btn,.prep-btn').forEach(button=>button.onclick=async()=>{const role=roleData.find(r=>r.id===button.dataset.roleId),jobId=role?jobIds.get(role.url):null;if(!authToken||!jobId){alert('Sign in before using automation. No request was sent.');return}const action=button.classList.contains('generate-btn')?'generate_application':'interview_prep';const previous=button.textContent;button.disabled=true;button.textContent='Working…';automationStatus.textContent='Sending a draft-only request…';try{const response=await api('/api/ai-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,job_id:jobId})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||`HTTP ${response.status}`);automationStatus.textContent='Request accepted. Review the generated material before using it.';alert('Request accepted. Review the generated draft or preparation material before using it.')}catch(error){automationStatus.textContent=`Automation request failed: ${error.message}`;alert(`Automation request failed: ${error.message}`)}finally{button.disabled=false;button.textContent=previous}});
</script><script>
/* August 13 status/category view contract. */
const viewGroupBy=document.querySelector('#group-by');
let expandedGroups=new Set();
function savePreferences(){localStorage.setItem(preferenceKey,JSON.stringify({q:q.value,fit:fit.value,work:work.value,source:source.value,verification:verification.value,stage:stageFilter.value,sort:sort.value,group:viewGroupBy.value}))}
function restorePreferences(){try{const p=JSON.parse(localStorage.getItem(preferenceKey)||'{}');q.value=p.q||'';fit.value=p.fit||'';work.value=p.work||'';source.value=p.source||'';verification.value=p.verification||'';stageFilter.value=p.stage||'';sort.value=p.sort||'score';viewGroupBy.value=p.group==='category'?'category':'status'}catch{localStorage.removeItem(preferenceKey)}}
function updateSummary(){const stages=['New','Review','Ready','Applied','Interview','Offer','Rejected'],counts=Object.fromEntries(stages.map(stage=>[stage,0]));cards.forEach(card=>{const stage=card.querySelector('.stage-value')?.textContent||'New';if(counts[stage]!==undefined)counts[stage]++});stageSummary.textContent=stages.filter(stage=>counts[stage]).map(stage=>`${stage} ${counts[stage]}`).join(' · ');document.querySelectorAll('[data-count-for]').forEach(node=>{const n=counts[node.dataset.countFor]||0;node.textContent=n?`(${n})`:''})}
function renderGrouping(){const statusMode=viewGroupBy.value==='status';document.querySelector('#status-view').classList.toggle('hidden',!statusMode);document.querySelector('#disposed').classList.toggle('hidden',!statusMode);document.querySelector('#category-view').classList.toggle('hidden',statusMode);cards.forEach(card=>{const stage=card.querySelector('.stage-value')?.textContent||'New';const target=statusMode?document.querySelector(`[data-status-group="${stage}"]`):document.querySelector(`#${card.dataset.lane} .grid`);if(target)target.appendChild(card)})}
function applyCollapse(){if(viewGroupBy.value!=='status'){cards.forEach(card=>card.classList.remove('collapsed-hidden'));return}document.querySelectorAll('[data-status-group]').forEach(grid=>{const key=grid.dataset.statusGroup,visible=[...grid.children].filter(card=>!card.classList.contains('hidden')),expanded=expandedGroups.has(key);visible.forEach((card,index)=>card.classList.toggle('collapsed-hidden',!expanded&&index>=3));const button=document.querySelector(`.collapse-btn[data-group="${key}"]`);if(button){button.hidden=visible.length<=3;button.textContent=expanded?'Show less':'Show more'}})}
function filter(){const s=q.value.toLowerCase(),f=+fit.value,w=work.value,src=source.value,ver=verification.value,stage=stageFilter.value;renderGrouping();const visibleCards=cards.filter(card=>{const matches=(!s||card.innerText.toLowerCase().includes(s))&&(!f||+card.dataset.score>=f)&&(!w||card.dataset.work===w)&&(!src||card.dataset.sourceGroup===src)&&(!ver||card.dataset.verification===ver)&&(!stage||card.querySelector('.stage-value')?.textContent===stage);card.classList.toggle('hidden',!matches);return matches});visibleCards.sort((a,b)=>{const stages=['New','Review','Ready','Applied','Interview','Offer','Rejected'];if(sort.value==='company')return a.querySelector('.company').textContent.localeCompare(b.querySelector('.company').textContent);if(sort.value==='stage')return stages.indexOf(a.querySelector('.stage-value')?.textContent)-stages.indexOf(b.querySelector('.stage-value')?.textContent);if(sort.value==='posted')return (b.querySelector('.meta')?.textContent||'').localeCompare(a.querySelector('.meta')?.textContent||'');return Number(b.dataset.score)-Number(a.dataset.score)});visibleCards.forEach(card=>card.parentElement.appendChild(card));resultCount.textContent=`${visibleCards.length} of ${cards.length} roles shown`;updateSummary();applyCollapse();savePreferences()}
restorePreferences();
q.oninput=fit.onchange=work.onchange=source.onchange=verification.onchange=stageFilter.onchange=sort.onchange=viewGroupBy.onchange=filter;
document.querySelectorAll('.collapse-btn').forEach(button=>button.onclick=()=>{const key=button.dataset.group;if(expandedGroups.has(key))expandedGroups.delete(key);else expandedGroups.add(key);applyCollapse()});
document.querySelectorAll('[data-view-link]').forEach(link=>link.onclick=()=>{viewGroupBy.value=link.dataset.viewLink;filter()});
document.querySelector('#clear').onclick=()=>{q.value=fit.value=work.value=source.value=verification.value=stageFilter.value='';sort.value='score';viewGroupBy.value='status';expandedGroups.clear();localStorage.removeItem(preferenceKey);filter()};
filter();
</script><script>
document.querySelectorAll('.card').forEach(card=>{
  const facts=document.createElement('div');facts.className='face-facts';
  const add=(text,cls='')=>{const el=document.createElement('span');el.className='face-fact '+cls;el.textContent=text;facts.append(el)};
  add((card.dataset.score||'')+'% match','score');
  [...card.querySelectorAll('.meta span')].slice(0,3).forEach(el=>add(el.textContent.replace(/^[📍📅🔎]\s*/,'').trim()));
  const actions=document.createElement('div');actions.className='face-actions';
  const details=document.createElement('button');details.type='button';details.textContent='View details';
  const listing=document.createElement('a');listing.className='primary';listing.href=card.querySelector('.primary-link')?.href||'#';listing.target='_blank';listing.rel='noopener';listing.textContent='Open listing';
  const stage=document.createElement('span');stage.className='face-stage';stage.textContent=card.querySelector('.stage-value')?.textContent||'New';
  actions.append(details,listing,stage);card.append(facts,actions);
  const modal=document.createElement('div');modal.className='job-modal';modal.innerHTML='<div class="job-modal-panel" role="dialog" aria-modal="true"><div class="job-modal-head"><div><div class="job-modal-company"></div><h2></h2></div><button class="job-modal-close" aria-label="Close details">×</button></div><div class="job-modal-body"></div><div class="job-modal-note">Review the listing and requirements before applying. Nothing is submitted automatically.</div></div>';
  modal.querySelector('h2').textContent=card.querySelector('.title')?.textContent||'';modal.querySelector('.job-modal-company').textContent=card.querySelector('.company')?.textContent||'';
  const body=modal.querySelector('.job-modal-body');['meta','why','tags','audit','role-controls','links'].forEach(cls=>{const source=card.querySelector('.'+cls);if(source)body.append(source.cloneNode(true))});document.body.append(modal);
  details.onclick=()=>{modal.classList.add('open');modal.querySelector('.job-modal-close').focus()};const close=()=>modal.classList.remove('open');modal.querySelector('.job-modal-close').onclick=close;modal.onclick=e=>{if(e.target===modal)close()};modal.onkeydown=e=>{if(e.key==='Escape')close()};
});
</script><script>
/* Category view is the default; status view remains available through Group by. */
const categorySections=[...document.querySelectorAll('#category-view .lane')];
categorySections.forEach(section=>{
  const grid=section.querySelector('.grid'); if(!grid) return;
  const roles=[...grid.querySelectorAll('.card')]; if(roles.length<=3) return;
  const wrap=document.createElement('div'); wrap.className='category-collapse';
  const button=document.createElement('button'); button.type='button'; button.textContent=`Show more (${roles.length-3})`;
  wrap.append(button); section.insertBefore(wrap,grid);
  let expanded=false;
  const render=()=>{roles.forEach((role,index)=>role.classList.toggle('category-hidden',!expanded&&index>=3));button.textContent=expanded?'Show less':`Show more (${roles.length-3})`};
  button.onclick=()=>{expanded=!expanded;render()}; render();
});
if(typeof viewGroupBy!=='undefined'){
  viewGroupBy.value='category';
  localStorage.setItem(preferenceKey,JSON.stringify({...JSON.parse(localStorage.getItem(preferenceKey)||'{}'),group:'category'}));
  if(typeof filter==='function') filter();
}
</script></body></html>